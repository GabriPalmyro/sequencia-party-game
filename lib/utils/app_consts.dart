class AppConsts {
  static const int minPlayersToStart = 5;
  static const int maxPlayers = 12;
}

const Duration kSplashScreenDuration = Duration(seconds: 2);
const Duration kSnackBarDuration = Duration(seconds: 3);
const Duration kDefaultAnimationDuration = Duration(milliseconds: 350);
const Duration kDefaultAnimationInterval = Duration(milliseconds: 200);
const Duration kDefaultAnimationDelay = Duration(milliseconds: 100);
